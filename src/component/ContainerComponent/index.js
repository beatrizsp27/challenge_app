import React from 'react';

// eslint-disable-next-line react/prop-types
const ContainerComponent = ({ children }) => {
    return <div className='screens_component_div'>{children}</div>;
};

export default ContainerComponent;
